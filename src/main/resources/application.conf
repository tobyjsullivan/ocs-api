api {
  host = "0.0.0.0"
  host = ${?HOST}
  port = 8080
  port = ${?PORT}

  cors.allowedOrigin = "http://localhost:3000"
  cors.allowedOrigin = ${?FRONT_END_HOSTNAME}
}

ordersSvc {
  host = ${OCS_ORDERS_SERVICE_HOST}
  port = ${OCS_ORDERS_SERVICE_PORT}
}